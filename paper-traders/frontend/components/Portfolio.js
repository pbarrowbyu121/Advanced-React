import Order from "./Order";

export default function Portfolio({ portfolio }) {
    // return <div><p>{portfolio.name}</p>{portfolio.orders.map((order) => (
    //     <Order key={order.id}
    //     order={order}/>
    // ))}</div>
    return <div><p>{portfolio.name}</p></div>
}

