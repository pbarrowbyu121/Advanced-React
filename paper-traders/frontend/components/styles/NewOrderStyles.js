import styled from "styled-components";

const NewOrderStyles = styled.div`
  background-color: var(--offWhite);
  position: fixed;
  /* position: relative; */
  height: 100%;
  width: 25vw;
  z-index: 10;
  top: 0;
  right: 0;
`;

export default NewOrderStyles;
