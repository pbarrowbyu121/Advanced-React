import styled from "styled-components";

const ChartTimeFilterStyles = styled.div`
  background: var(--lightGray);
  margin-top: 4rem;
  margin-left: 11rem;
  width: 29%;
  border-color: var(--gray);
  border: solid;
  label {
    padding-left: 3rem;
  }
`;

export default ChartTimeFilterStyles;
